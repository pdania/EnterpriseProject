<Window x:Class="UI.Views.DashBoardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Views"
        mc:Ignorable="d"
        Title="DashBoardView" Height="450" Width="800">
    <Grid >
        
        <StackPanel Margin="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Log out" Command="{Binding LoginCommand}" HorizontalAlignment="Right"/>
                <Label Content="Main menu" HorizontalAlignment="Center" VerticalContentAlignment="Bottom"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,40,0,0">
                <Button Content="New Random" Width="140" Height="40" Command="{Binding RandomCommand}"/>
                <Button Content="History" Width="140" Height="40" Margin="120,0,0,0" Command="{Binding HistoryCommand}"/>
            </StackPanel>
           
        </StackPanel>
    </Grid>
</Window>
